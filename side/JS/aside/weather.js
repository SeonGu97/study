'use strict';

export default class Weather {
    constructor(Create, _aside) {
        const _weather = new Create('weather', 'div', 'class', 'weather basics hide-s-y un-transparent', _aside);
    }
}