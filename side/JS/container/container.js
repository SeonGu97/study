'ust strict';

export default class Container {
    constructor(Create, _app) {
        // create
        const _container = new Create('container', 'section', 'class', 'container basics', _app);
    }
}